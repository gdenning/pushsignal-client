<?xml version="1.0" encoding="utf-8"?>
<TabHost xmlns:android="http://schemas.android.com/apk/res/android"
	android:id="@android:id/tabhost" android:layout_width="fill_parent"
	android:layout_height="fill_parent">
	<RelativeLayout android:orientation="vertical"
		android:layout_width="fill_parent" android:layout_height="fill_parent"
		android:padding="5dp">
		<TabWidget android:layout_alignParentTop="true" android:id="@android:id/tabs"
			android:layout_width="fill_parent" android:layout_height="wrap_content"
			android:layout_below="@+id/username" />
		<FrameLayout android:layout_height="fill_parent"
			android:layout_width="fill_parent" android:id="@android:id/tabcontent"
			android:layout_below="@android:id/tabs" android:layout_above="@+id/divider"
			android:layout_alignLeft="@android:id/tabs"
			android:layout_alignRight="@android:id/tabs"></FrameLayout>
		<View android:id="@+id/divider" android:layout_width="fill_parent"
			android:layout_above="@+id/username" android:layout_height="2dip"
			android:background="#FF000000" />
		<TextView android:layout_alignParentBottom="true"
			android:layout_alignParentLeft="true" android:id="@+id/username"
			android:text="Attempting to register with server..."
			android:layout_height="wrap_content" android:layout_width="wrap_content"></TextView>
		<TextView android:layout_alignParentBottom="true"
			android:layout_alignParentRight="true" android:text=""
			android:layout_height="wrap_content" android:layout_width="wrap_content"
			android:id="@+id/points"></TextView>
	</RelativeLayout>
</TabHost>